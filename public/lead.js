(()=>{const endpoint='https://ai-search-audit-dun.vercel.app/api/lead';const form=document.querySelector('#paid-lead-form');const status=document.querySelector('#lead-status');if(!form||!status)return;form.addEventListener('submit',async e=>{e.preventDefault();status.textContent='Sending…';const data=new FormData(form);const body={email:String(data.get('email')||''),need:String(data.get('need')||''),leadVariant:'quick',site:String(data.get('site')||''),website:String(data.get('website')||''),consent:data.get('consent')==='yes'};try{const r=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body),credentials:'omit'});const result=await r.json().catch(()=>({}));if(!r.ok||result.ok!==true)throw new Error(result.error||'Submission failed');status.textContent='Inquiry received. We will review it and contact you by email. No payment or contract has been created.';form.reset();}catch(err){status.textContent='The hosted form could not be submitted. Please use one of the GitHub inquiry links below as a fallback.';}});})();
